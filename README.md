Real Cricket Career V9.2 — Match Engine Reliability Fix

This build keeps the existing UI and career systems. The Three.js engine is now optional at runtime. If the CDN cannot load, the game automatically uses the built-in 2D safe match view instead of entering Safe Recovery Mode.

Replace the whole project folder; no code edits required.
